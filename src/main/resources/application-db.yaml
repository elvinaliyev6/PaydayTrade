spring:
  datasource:
    url: jdbc:h2:mem:paydaytrade
    driverClassName: org.h2.Driver
    username: elvin
    password: 12345
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
  h2:
    console:
      enabled: true

